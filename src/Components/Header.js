import React from "react";
import Logo from "../Images/Logo";

export default function Header() {
    return(
        <div id='header'>
            <Logo />
            <h1>Todo List</h1>
        </div>
    )
}